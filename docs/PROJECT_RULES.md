# PROJECT_RULES

- 対象店舗は9店舗に固定する。
- 分析対象は全スロットとする。
- `machine_results` から `unit_results` を推定で埋めない。
- 差枚 `"-"` を `0` として扱わない。
- `0` と `NULL` を区別する。
- 欠損率50%以上なら台番・末尾・並び分析は信頼不可とする。
- レポートには欠損率・サンプル数・有効分析範囲を必ず出す。
- Codex の各タスク前に `docs` を読む。
